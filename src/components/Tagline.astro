---
import { GoCodeOfConduct } from "react-icons/go";
---

<div class="tag">
  <i
    class="icon"
    id="conduct"
    style="position: absolute; transform: rotate(21deg);"
    ><GoCodeOfConduct client:idle /></i
  >
  <p>
    <span class="tagline">Hey! I'm Rahul Marban, also known as{" "}</span><span
      class="highlight">Rahuletto.</span
    ><span class="tagline">
      An 18-year-old self-taught programmer.
      <br /><br />Websites? Backends? I've got them covered. Making technology
      more user-friendly and accessible, like transforming code into magic
      tricks for everyone.
    </span>
  </p>
</div>

<style>
  #conduct {
    right: 0px;
    margin-top: -36px;
  }

  #conduct svg {
    font-size: 46px;
  }
  .tag {
    position: relative;
    background: var(--box);
    border-radius: 32px;
    border: 1px var(--border) solid;
    margin: 24px auto;
    max-width: 88vw;
    padding: 22px 30px;
    transition: 0.3s cubic-bezier(0.35, 0.76, 0.4, 0.92);
    -webkit-transition: 0.3s cubic-bezier(0.35, 0.76, 0.4, 0.92);
    -moz-transition: 0.3s cubic-bezier(0.35, 0.76, 0.4, 0.92);
    -ms-transition: 0.3s cubic-bezier(0.35, 0.76, 0.4, 0.92);
    -o-transition: 0.3s cubic-bezier(0.35, 0.76, 0.4, 0.92);
  }
  .tag p {
    font-size: 18px;
    margin: 0;
    max-width: 900px;
  }
  .highlight {
    color: #8a5ad0;
    font-family: "Outfit Variable", system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
    font-weight: 700;
    word-wrap: break-word;
  }
  .tagline {
    color: var(--color);
    font-family: "Space Grotesk Variable", system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
    font-weight: 400;
    word-wrap: break-word;
  }

  @media only screen and (max-width: 700px) {
    #conduct svg {
      font-size: 32px;
    }
  }
  @media only screen and (max-width: 600px) {
    .tag {
      width: auto;
      margin: 24px;
    }

    .tag p {
      font-size: 16px;
    }
  }
</style>
